# ─── Bybit Scalping Bot — Dependencies ───────────────────────────
# pip install -r requirements.txt

# Async networking
aiohttp>=3.9.0
websockets>=12.0

# Bybit API client (alternatif olarak ccxt kullanılabilir)
pybit>=5.6.0

# Teknik analiz (TA-Lib kurulumu için: brew install ta-lib / apt-get install libta-lib-dev)
# TA-Lib kurulumu başarısızsa numpy fallback kullanılır
TA-Lib>=0.4.28

# Data
numpy>=1.24.0
pandas>=2.0.0

# ML
scikit-learn>=1.3.0
torch>=2.0.0          # opsiyonel, policy network için

# Yardımcı
python-dotenv>=1.0.0
